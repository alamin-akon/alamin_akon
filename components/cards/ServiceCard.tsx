import { ArrowUpRight, Code2, MousePointer2, Palette, PanelsTopLeft, RefreshCw, Settings2, ShoppingBag, Store } from "lucide-react";
import type { Service } from "@/types";
const icons = { Palette, Code2, PanelsTopLeft, Store, Settings2, ShoppingBag, MousePointer2, RefreshCw };
export function ServiceCard({ service, featured = false }: { service: Service; featured?: boolean }) { const Icon = icons[service.icon as keyof typeof icons] ?? Code2; return <article className={`group relative rounded-3xl border p-6 transition duration-300 ${featured ? "border-primary/35 bg-primary/[.09]" : "border-white/8 bg-white/[.025] hover:-translate-y-1 hover:border-white/18 hover:bg-white/[.045]"}`}><div className="flex items-start justify-between"><div className="grid size-11 place-items-center rounded-2xl bg-gradient-to-br from-primary/30 to-secondary/25 text-accent"><Icon className="size-5" /></div><ArrowUpRight className="size-5 text-text-secondary transition group-hover:-translate-y-1 group-hover:translate-x-1 group-hover:text-accent" /></div><h3 className="mt-7 text-xl font-bold tracking-tight text-white">{service.title}</h3><p className="mt-3 text-sm leading-6 text-text-secondary">{service.description}</p><ul className="mt-5 space-y-2">{service.features.slice(0, 3).map((feature) => <li key={feature} className="flex items-center gap-2 text-xs text-slate-300"><span className="size-1 rounded-full bg-accent"/>{feature}</li>)}</ul></article>; }
