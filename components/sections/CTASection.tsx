import { ArrowRight } from "lucide-react";
import { Button } from "@/components/ui/Button";
import { Container } from "@/components/ui/Container";
export function CTASection({ title = "Let’s Build Something Professional", description = "From Shopify stores to modern business websites, I can help transform your idea into a responsive and user-friendly digital experience.", secondary = true }: { title?: string; description?: string; secondary?: boolean }) { return <section className="pb-24 pt-8 sm:pb-32"><Container><div className="relative overflow-hidden rounded-[2rem] border border-primary/30 bg-gradient-to-br from-primary/25 via-surface to-secondary/20 px-6 py-12 sm:px-12 sm:py-16"><div className="absolute -right-20 -top-24 size-72 rounded-full bg-accent/20 blur-3xl"/><div className="relative max-w-3xl"><p className="text-xs font-bold uppercase tracking-[.2em] text-accent">Start a conversation</p><h2 className="mt-4 text-3xl font-bold tracking-[-.05em] text-white sm:text-5xl">{title}</h2><p className="mt-5 max-w-2xl leading-7 text-slate-300">{description}</p><div className="mt-8 flex flex-wrap gap-3"><Button href="/contact">Start a Project <ArrowRight className="size-4"/></Button>{secondary && <Button href="/projects" variant="secondary">View My Work</Button>}</div></div></div></Container></section>; }
