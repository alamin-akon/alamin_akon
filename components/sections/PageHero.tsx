import type { ReactNode } from "react";
import { Container } from "@/components/ui/Container";
export function PageHero({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children?: ReactNode }) { return <section className="relative overflow-hidden border-b border-white/8 pb-16 pt-36 sm:pb-20 sm:pt-44"><div className="hero-grid absolute inset-0 opacity-30"/><div className="absolute -right-28 top-0 size-96 rounded-full bg-primary/15 blur-[110px]"/><Container className="relative"><p className="text-xs font-bold uppercase tracking-[.2em] text-accent">{eyebrow}</p><h1 className="mt-5 max-w-4xl text-4xl font-extrabold tracking-[-.06em] text-white sm:text-6xl">{title}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-text-secondary sm:text-lg">{description}</p>{children}</Container></section>; }
