"use client";

import { useState, type ReactNode } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { CheckCircle2, LoaderCircle, Send } from "lucide-react";
import { contactSchema, type ContactFormValues } from "@/lib/validations";
const projectTypes = ["Website Design", "Website Development", "Shopify Store Design", "Shopify Customisation", "UI/UX Design", "Landing Page", "Website Redesign", "Other"];
const budgets = ["Under £500", "£500–£1,000", "£1,000–£2,500", "£2,500–£5,000", "More than £5,000", "Not decided yet"];
export function ContactForm() { const [status, setStatus] = useState<"idle" | "success" | "error">("idle"); const [message, setMessage] = useState(""); const { register, handleSubmit, formState: { errors, isSubmitting }, reset } = useForm<ContactFormValues>({ resolver: zodResolver(contactSchema) }); const submit = async (values: ContactFormValues) => { setStatus("idle"); try { const response = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); const data: unknown = await response.json(); if (!response.ok || typeof data !== "object" || data === null || !("message" in data)) throw new Error("Unable to send your message."); setStatus("success"); setMessage(String(data.message)); reset(); } catch (error) { setStatus("error"); setMessage(error instanceof Error ? error.message : "Something went wrong. Please try again or contact me through social media."); } }; const field = "mt-2 w-full rounded-xl border border-white/10 bg-background/65 px-4 py-3 text-sm text-white outline-none transition placeholder:text-slate-600 focus:border-accent focus:ring-2 focus:ring-accent/20"; return <form noValidate onSubmit={handleSubmit(submit)} className="rounded-3xl border border-white/10 bg-surface/60 p-5 shadow-2xl shadow-black/10 sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><Field label="Full name" error={errors.name?.message}><input className={field} autoComplete="name" placeholder="Your name" {...register("name")}/></Field><Field label="Email address" error={errors.email?.message}><input className={field} type="email" autoComplete="email" placeholder="you@example.com" {...register("email")}/></Field><Field label="Company or brand" error={errors.company?.message}><input className={field} autoComplete="organization" placeholder="Optional" {...register("company")}/></Field><Field label="Project type" error={errors.projectType?.message}><select className={field} defaultValue="" {...register("projectType")}><option value="" disabled>Select a service</option>{projectTypes.map((type) => <option className="bg-surface" key={type}>{type}</option>)}</select></Field><Field label="Estimated budget" error={errors.budget?.message}><select className={field} defaultValue="" {...register("budget")}><option value="" disabled>Select a budget</option>{budgets.map((budget) => <option className="bg-surface" key={budget}>{budget}</option>)}</select></Field></div><Field label="Tell me about your project" error={errors.message?.message} className="mt-5"><textarea className={`${field} min-h-32 resize-y`} placeholder="What would you like to create or improve?" {...register("message")}/></Field>{status !== "idle" && <p role="status" className={`mt-5 flex gap-2 rounded-xl px-4 py-3 text-sm ${status === "success" ? "bg-emerald-500/10 text-emerald-300" : "bg-rose-500/10 text-rose-300"}`}>{status === "success" && <CheckCircle2 className="size-5 shrink-0"/>}{message}</p>}<button type="submit" disabled={isSubmitting} className="mt-6 inline-flex min-h-11 items-center gap-2 rounded-full bg-primary px-5 text-sm font-semibold text-white transition hover:bg-primary/90 disabled:cursor-not-allowed disabled:opacity-70">{isSubmitting ? <><LoaderCircle className="size-4 animate-spin"/>Sending...</> : <><Send className="size-4"/>Send Enquiry</>}</button></form>; }
function Field({ label, error, children, className = "" }: { label: string; error?: string; children: ReactNode; className?: string }) { return <label className={`block ${className}`}><span className="text-sm font-semibold text-slate-200">{label}</span>{children}{error && <span className="mt-2 block text-xs text-rose-300">{error}</span>}</label>; }
